export default function Loading() {
  return (
    <main className="content-wrapper">
      <p style={{ marginTop: 16, color: 'var(--text-secondary)', fontSize: 'var(--text-sm)' }}>
        Loading...
      </p>
    </main>
  );
}
